# Two_Pointer_Approach

Q01.Reverse the given array in place.
Q02.Check if Array is Palindrome.
Q03.Check if a string is a Palindrome ignoring non-alphabetic characters.
Q04.Check if two sorted arrays are equal (element-wise).
Q05.Move all zeroes to the end (stable order).
Q06.Remove all occurrences of a given value (in-place).
Q07.Separate even and odd numbers (evens first).
Q08.Remove duplicates from sorted array in-place and return new length.
Q09.Split array into positive and negative numbers (in-place)